server:
    port: 8080

spring:
    cloud:
        config:
            server:
                git:
                    default-label: master
                    uri: https://github.com/khebinSd/KrugerSFinalTask
                    searchPaths: config-data
    security:
        user:
            name: root
            password: s3cr3t

