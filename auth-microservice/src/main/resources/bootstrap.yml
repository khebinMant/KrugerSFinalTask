spring:
    application:
        name: auth-microservice
    cloud:
        config:
            uri: http://localhost:8080
            username: root
            password: s3cr3t
jwt:
    secret: secret