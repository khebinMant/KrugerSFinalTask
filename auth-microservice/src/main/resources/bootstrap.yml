spring:
    application:
        name: auth-microservice
    cloud:
        config:
            uri: http://localhost:8080
            username: root
            password: s3cr3t
jwt:
    secret: secret

admin-paths:
    paths:
        -
            uri: "/customers"
            method: "POST"